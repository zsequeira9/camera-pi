# camera-pi

## Installation

1. Install [Docker](https://docs.docker.com/engine/install/debian/)

2. Set device number in .env
eg. if device is located at /dev/video0, set DEVICE=0

3. Run 'sudo docker compose up'

4. Stream is located at <raspberry pi over LAN url>:8080
